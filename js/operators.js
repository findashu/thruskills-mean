// Assignment Operators

    // 1.   Assignment x = y
    // 2.   Addition Assignment x+=y
    // 3.   Substraction Assignment x-=y
    // 4.   Multiplication Assignment x*=y
    // 5.   Division Assignment x/=y
    // 6.   Remainder Assignment x%=y

// Comparison Operators

    // 1. Equals ==
    // 2. Not Equals !=
    // 3. Strict Equals ===
    // 4. Strict Not Equal !==
    // 5. Greater than >
    // 6. Greater than or equal >=
    // 7. Less than or equal <=

// Arithmetic Operators
    // Addition +
    // Substraction -
    // Multiplication *
    // Division /
    // 1. Remainder %
    // 2. Increment ++
    // 3. Decrement --
    // 4. Unary negation -x(It also precedes the operand and converts non-numbers data types to numbers like unary plus, however, it performs an additional operation, negation)
    // 5. Unary Plus +x (This operator precedes the operand and tries to convert it to a number)

// Logical Operators

    // 1. Logical AND &&
    // 2. Logical OR ||
    // 3. Logical Not !

// String Operator
    Concatenation

// Ternary Operator
    condition ? val1 : val2

// Comma Operator

// typeof
// instanceof

// Falsy Values

    // 1. The empty string ''
    // 2. null
    // 3. undefined
    // 4. The number 0
    // 5. The number NaN
    // 6. The Boolean false


// Strict Comparision(===) Vs Comparision (==);

    // When using triple equals === in JavaScript, we are testing for strict    equality. This means both the type and the value we are comparing have      to be the same.

    // When using == in JS, we are comparing only values

    // 'hello world' === 'hello world'
    // true (Both Strings, equal values)
    // true === true
    // true (Both Booleans, equal values)

    // 77 == '77'